# Optional local development only. Prefer entering a key in Graphly's UI.
# Get a key: https://aistudio.google.com/apikey
# VITE_ values can be embedded in public browser bundles.
# Leave this empty for production builds; never commit a real key.
VITE_GEMINI_API_KEY=
